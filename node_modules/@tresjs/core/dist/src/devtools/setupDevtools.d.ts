import { TresContext } from '../composables';
export interface PerformanceState {
    maxFrames: number;
    fps: {
        value: number;
        accumulator: number[];
    };
    memory: {
        currentMem: number;
        allocatedMem: number;
        accumulator: number[];
    };
}
export declare function setupTresDevtools(ctx: TresContext): void;
